apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-zk-server-primary
spec:
  template:
    spec:
      containers:
        - name: zookeeper
status:
  availableReplicas: ($available_replicas)
  replicas: 1
