apiVersion: zookeeper.kubedoop.dev/v1alpha1
kind: ZookeeperCluster
metadata:
  name: test-zk-metrics
spec:
  image:
    productVersion: ($values.product_version)
  servers:
    roleGroups:
      default:
        replicas: 1
