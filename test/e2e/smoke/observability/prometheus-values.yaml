# Simple Prometheus configuration for e2e testing
server:
  persistentVolume:
    enabled: false

alertmanager:
  enabled: false

prometheus-node-exporter:
  enabled: false

prometheus-pushgateway:
  enabled: false

kube-state-metrics:
  enabled: false
