apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-zk-server-default
spec:
  template:
    spec:
      containers:
        - name: server
status:
  availableReplicas: ($available_replicas)
  replicas: ($available_replicas)
